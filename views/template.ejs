<!DOCTYPE html>
<html>
    <head>
        <title>EJS</title>
        <script>
            const state = {'page_id': 1}
            const title = ''
            const url =  'user.html' 
            history.pushState(state, title, url)
            //can clear history with 
                // for(var i = 0; i < history.length; i++){
                //     history.pushState({},'');
                // };
        </script>
    </head>
    <body>
        <h1 style="position: absolute; top: 10px; right: 600px;"> <%= data.name %></h1>
        <img src="<%= data.pic %> " width="250px" style="position:absolute; right: 550px; bottom: 100px;">
    </body>
</html>